#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Decription: This function prints the desired text.
 *
 * Return: Always 0 (success)
 */
int main(void)
{
printf("with propper grammar, but the outcome is a piece of art,");
return (0);
}
